apiVersion: v1
kind: Service
metadata:
  name: wild-tour
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: wild-tour
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
